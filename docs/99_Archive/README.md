# 99_Archive 废弃与归档库

如果你在主目录中没有找到某个系统的文档，它大概率在经历了一次底层大重组（重构）后被移到了这里。

**规则：**
1. **只进不出**：这里的文档是架构的“历史快照”。
2. **不允许修改旧逻辑**：存档后的文档即使有错字或代码不兼容也不用管。
3. 请配合 `99_Logs_and_Refactors` 中的 ADR 架构决议一起阅读，以了解“为什么这段代码最终被淘汰”。