# 99_Logs_and_Refactors 架构决策记录 (ADR)

每一次伤筋动骨的大重构，都在这里留下一篇记录。

每次重构需至少回答三个关键问题：
1. **Context (当时的背景与瓶颈)**：原有的架构出了什么问题？（例如：帧率过低、API 太杂乱、无法跨平台等）。
2. **Decision (新的决策)**：决定如何推翻重写？采用了什么新的设计模式？
3. **Consequences (重构的结果)**：新的架构带来了什么优势，又做出了什么样的妥协？